var config = {
  user: '', // reittiopas API username & password
  pass: '',

  api_key: '', // open 311 API key

  db_name: '', // database name / username / password
  db_user: '',
  db_passwd: ''
};

module.exports = config;